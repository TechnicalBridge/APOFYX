# APOFYX — dependencias del proyecto
#
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Verificado en Python 3.14.3 (Windows) el 15-09-2026.
# Django 6.x es obligatorio: las series 5.x no soportan Python 3.14.

# --- Framework web ---
Django==6.1.1

# --- Conector MySQL ---
# Este equipo ya tiene mysqlclient 2.2.8 funcionando sobre Python 3.14,
# por eso se usa este y no PyMySQL. Si la instalacion fallara por falta de
# compilador, la alternativa sin compilar es:  PyMySQL==1.2.0
mysqlclient>=2.2.8

# --- Configuracion ---
# Credenciales de MySQL, SECRET_KEY y claves del LLM fuera del codigo, en .env
python-dotenv==1.2.2

# --- Archivos estaticos ---
# Con DEBUG=0 Django no sirve los estaticos: espera un nginx delante. En el
# contenedor no lo hay, y WhiteNoise los sirve desde el mismo proceso.
whitenoise==6.12.0

# --- Respaldo del asistente (D4: hibrido reglas + LLM) ---
# Google Gemini, tier gratuito de AI Studio. Solo requiere GEMINI_API_KEY
# en el .env: sin descargas, sin GPU, sin servicios que levantar.
#
# Se usa unicamente cuando el motor de reglas no alcanza el umbral de
# confianza. Sin GEMINI_API_KEY el asistente funciona igual: cae en la
# respuesta de fallback guardada en la base de datos.
google-genai==2.23.0

# --- Desarrollo ---
# Mide que porcentaje del codigo ejecutan las pruebas:
#   python -m coverage run --source=crm,assistant,cartera,integracion,config --omit="*/migrations/*,*/tests.py" manage.py test
#   python -m coverage report -m
coverage==7.16.1
